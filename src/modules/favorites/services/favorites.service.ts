/**
 * Placeholder: cuando exista API de favoritos, implementar aquí
 * (get/list/toggle) usando `api` de `@/src/api/base`.
 */
export const FavoritesService = {
  // getAll: async () => { ... },
  // toggle: async (id: string) => { ... },
};
